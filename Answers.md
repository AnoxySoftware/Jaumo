### Q) Your full name?
`Lefteris Haritou`

### Q) How long did you spend on the homework test?
`5 hours to complete the code and 1 hour in Unit Testing`

### Q) What would you add to your solution if you had more time? If you didn't spend much time on the coding test then use this as an opportunity to explain what you would add.
`If I'd spend more time I'd add a custom Transition when showing the Modal VC from the UICollectionView so that it is zooming in from the grid cell to the full screen`

### Q) Which parts did you spend the most time with?
`The Custom Swip Gesture to close the Modal VC`

### Q) How did you find the test overall? We’d love to hear your suggestions on how we can improve.

`The test was easy to understand and complete, however the Provided Mockups cannot be acurately recreated without providing font information and because UINames data are not providing some fields found on the mockup.`
`Also I'd like to get an API where I'd get the data partially, so you can evaluate if the developer would choose to use paging to fetch data instead of fetching a huge dataset at once`